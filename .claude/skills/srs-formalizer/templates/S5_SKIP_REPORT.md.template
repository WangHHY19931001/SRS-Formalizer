# S5 形式化阶段 — 跳过报告

> 此文件在 S5 阶段被跳过时自动生成。S6 验收闸门仍会正常运行。

## 跳过判定

| 模块 | 触发状态 | 原因 |
|------|:--------:|------|
| TLA+ 建模 | `{{S5_TLA_TRIGGER}}` | {{TLA_REASON}} |
| Lean 4 证明 | `{{S5_LEAN_TRIGGER}}` | {{LEAN_REASON}} |

## 影响分析

- **S6 验收闸门**：S6 不依赖 S5 产物（TLA+ 图谱、Lean 证明图谱在 S6 中标记为 N/A 而非 FAIL）
- **跨图一致性验证**：Q4-Q9 中涉及 TLA+/Lean 的问题将标记为 N/A
- **后续可恢复**：如需未来添加形式化，从 S0 重新判定触发条件并重新运行 S5
